specVersion: "3.2"
metadata:
  name: managealerts
type: application
builder:
  customTasks:
  - name: ui5-task-zipper
    afterTask: generateCachebusterInfo
    configuration:
      archiveName: managealerts
      additionalFiles:
      - xs-app.json
