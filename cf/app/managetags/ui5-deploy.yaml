specVersion: "3.2"
metadata:
  name: managetags
type: application
builder:
  customTasks:
  - name: ui5-task-zipper
    afterTask: generateCachebusterInfo
    configuration:
      archiveName: managetags
      additionalFiles:
      - xs-app.json
