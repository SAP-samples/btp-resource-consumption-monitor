specVersion: "3.2"
metadata:
  name: measurestotal
type: application
builder:
  customTasks:
  - name: ui5-task-zipper
    afterTask: generateCachebusterInfo
    configuration:
      archiveName: measurestotal
      additionalFiles:
      - xs-app.json
