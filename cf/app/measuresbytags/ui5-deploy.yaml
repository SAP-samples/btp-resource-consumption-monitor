specVersion: "3.2"
metadata:
  name: measuresbytags
type: application
builder:
  customTasks:
  - name: ui5-task-zipper
    afterTask: generateCachebusterInfo
    configuration:
      archiveName: measuresbytags
      additionalFiles:
      - xs-app.json
